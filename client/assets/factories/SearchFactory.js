app.factory('SearchFactory', function() {
    $scope.getSearchResults = function(search) {
        var searchResults = {};
        searchResults = function(search) {
            return ; //get request here
        }
        return searchResults; 
    }
            
    
})
